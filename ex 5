// Зробіть автомобіль без ключа ще краще!
// Використовуємо рішення з минулого завдання.

var age = prompt("Скільки Вам років?");

if (Number(age) < 18) {
	alert("Вибачте, Ви занадто молодий, щоб керувати цим авто. Доступ заблоковано");
} else if (Number(age) > 18) {
	alert("Вітаємо з першим водійським досвідом! Приємної подорожі!");
} else if (Number(age) === 18) {
	alert("Двигун ввімкнено. Приємної подорожі!");
}

//1. Зробіть код зверху функцією checkDriverAge(). Функція запитує вік користувача, якщо її викликати. Використовуйте функціональні вирази, щоб створити цю функцію. 

var checkDriverAge =  function(){
            var age = prompt("Введіть ваш вік");

            if (Number(age) < 18) {
                alert("Вибачте, Ви занадто молодий, щоб керувати цим авто. Доступ заблоковано");
            } else if (Number(age) > 18) {
                alert("Двигун ввімкнено. Приємної подорожі!");
            } else if (Number(age) === 18) {
                alert("Вітаємо з першим водійським досвідом! Приємної подорожі!");
            }
        }

//2. Зробіть ще одну функцію, що робить те саме, але використовуючи оголошення функції.

checkDriverAge();
        function checkDriverAge1(){
            var age = prompt("Введіть ваш вік");

            if (Number(age) < 18) {
                alert("Вибачте, Ви занадто молодий, щоб керувати цим авто. Доступ заблоковано");
            } else if (Number(age) > 18) {
                alert("Двигун ввімкнено. Приємної подорожі!");
            } else if (Number(age) === 18) {
          
      alert("Вітаємо з першим водійським досвідом! Приємної подорожі!");
            }
        }

//3. Використайте console.log замість prompt та зробіть функцію checkDriverAge() так, щоб вона приймала вік як аргумент. Якщо прописати checkDriverAge(19) функція відповідає "Двигун ввімкнено. Приємної подорожі!"

checkDriverAge1();
        function checkDriverAge2(age){
            if (age< 18) {
                console.log("Вибачте, Ви занадто молодий, щоб керувати цим авто. Доступ заблоковано");
            } else if (age > 18) {
                console.log("Двигун ввімкнено. Приємної подорожі!");
            } else if (age === 18) {
                console.log("Вітаємо з першим водійським досвідом! Приємної подорожі!");
            }
        }
        checkDriverAge2(19);





